<% unless @recaptcha_result %>
  <%= turbo_stream.update 'recaptcha-v2' do %>
    <div class="mb-3 row recaptcha-form ml-2 mt-3"
      id='recaptchaV2'
      data-controller='recaptcha-v2'
      data-recaptcha-v2-site-key-value="<%= Rails.application.credentials.recaptcha_site_key_v2 %>"
      data-recaptcha-v2-url-value="<%= trailers_path %>">
    </div>
  <% end %>
<% end %>
<% if @trailer_lead.present? %>
  <%= turbo_stream.update 'job-applicant-form' do %>
    <%= render 'trailer', trailer: @trailer_lead %>
  <% end %>
<% end %>

<script src="https://www.google.com/recaptcha/api.js?onload=onloadCallback&render=explicit"></script>
